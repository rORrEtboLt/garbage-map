const APP_ROUTES = {
  AUTHENTICATION: "/authentication",
  API: "/api"
}

const AUTHENTICATION_ROUTES = {
  SIGN_UP: "/sign-up",
  LOGIN: "/login"
};

module.exports = { AUTHENTICATION_ROUTES, APP_ROUTES };
